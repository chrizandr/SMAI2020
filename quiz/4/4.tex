
\begin{frame}
\section{}
Consider a recurrent/feedback model of neural network given by
\[ s_t = f(Ux_t + W s_{t-1}) \]
\[ o_t = g(V s_t) \]

\begin{enumerate}[label=(\Alph*)]
\item $U$ is a set of learnable parameters.   % Ans
\item $W$ is a set of learnable parameters    % Ans
\item $s$ is a set of learnable parameters
\item BP identifies optimal (may be local) parameters for each $t$
\item None of the above.   % None
\end{enumerate}

\end{frame}

\begin{frame}
\section{}
Consider a recurrent/feedback model of neural network given by
\[ s_t = f(Ux_t + W s_{t-1}) \]
\[ o_t = g(V s_t) \]

\begin{enumerate}[label=(\Alph*)]
\item $V$ is a set of learnable parameters.   % Ans
\item $W$ is a set of learnable parameters    % Ans
\item $s$ is a set of learnable parameters
\item BP identifies optimal (may be local) parameters for each $t$
\item None of the above.   % None
\end{enumerate}
\end{frame}

\begin{frame}
\section{}
Consider a recurrent/feedback model of neural network given by
\[ s_t = f(Ux_t + W s_{t-1}) \]
\[ o_t = g(V s_t) \]

\begin{enumerate}[label=(\Alph*)]
\item $U$ is a set of learnable parameters.    % Ans
\item $V$ is a set of learnable parameters    % Ans
\item $s$ is a set of learnable parameters
\item BP identifies optimal (may be local) parameters for each $t$
\item None of the above.   % None
\end{enumerate}
\end{frame}

\begin{frame}
\section{}

Consider a recurrent/feedback model of neural network given by
\[ s_t = f(Ux_t + W s_{t-1}) \]
\[ o_t = g(V s_t) \]

\begin{enumerate}[label=(\Alph*)]
\item $U$ is a set of learnable parameters.   % Ans
\item $V$ is a set of learnable parameters    % Ans
\item $W$ is a set of learnable parameters    % Ans
\item BP can be adapted appropriately to learn the optimal weights.   % Ans
\item None of the above.   % None
\end{enumerate}
\end{frame}


\begin{frame}
\section{}
Consider a recurrent/feedback model of neural network given by
\[ s_t = f(Ux_t + W s_{t-1}) \]
\[ o_t = g(V s_t) \]

\begin{enumerate}[label=(\Alph*)]
\item The suffix $t$ is used to denote that such models are appropriate for processing sequences.
\item $f()$ and $g()$ are activation functions. (such as tanh or relu)
\item BP can be adapted appropriately to learn the optimal weights.
\item All of the above.   % None    % Ans
\end{enumerate}
\end{frame}
